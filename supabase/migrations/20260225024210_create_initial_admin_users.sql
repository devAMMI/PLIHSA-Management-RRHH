/*
  # Crear usuarios administradores iniciales

  1. Usuarios a crear
    - karla.sagastume@plihsa.com (Admin de PLIHSA)
    - Andrea.fuentes@plihsa.com (Admin de PLIHSA)
    - dev@ammi.com (Admin de AMMI)
  
  2. Nota
    - Los usuarios de Auth se deben crear via Auth Admin API
    - Esta migración solo prepara la estructura
    - Los usuarios se crearán mediante script o interfaz
*/

-- Esta migración prepara la base de datos
-- Los usuarios se crearán mediante el componente de usuarios o script
SELECT 1;
